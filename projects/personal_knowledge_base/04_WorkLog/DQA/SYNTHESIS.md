# DQA 지식 합성

> 마지막 갱신: 2026-04-14 19:36 | 기반 항목 수: 30

---

## 핵심 지식

1. **세션 간 컨텍스트 전달 방식**: 브라우저 익스텐션이 연결되지 않을 경우, 이전 세션(예: `session_01BmJwoQ8VR7J2YuHJipG9Qp`)의 내용을 직접 수동으로 확인하고 모델 설계 반영 여부를 검토해야 한다. 자동화 의존도가 높을수록 단절 시 수작업 비용이 증가한다.

2. **신규 브랜치 전략**: 후속 작업 아이템 진행 시 반드시 신규 브랜치를 생성하는 워크플로우가 표준으로 설정되어 있다. 모델 설계 검증 → 브랜치 분기 → 후속 아이템 구현 순서로 진행된다.

3. **Multi-Agent Ecosystem 설계 계획 승인**: `c5083451` 세션(2026-04-11)에서 Personal Knowledge Base의 도메인별 프로젝트(nova_he* 등) 구조를 기반으로 한 멀티에이전트 생태계 설계 플랜이 승인(approved)된 상태다.

4. **sv_lakehouse 환경 인증 문제**: `46c5af1b` 세션(2026-04-07)에서 `.venv` 활성화 후 API 401 인증 오류(`/login` 요구)가 반복 발생했다. Claude Code CLI 환경에서 API 키 또는 세션 토큰 재인증이 필요한 상황이다.

5. **로그 중복 저장 문제**: 동일 파일(`002cd1b4`, `c5083451`, `46c5af1b`)의 대화가 각각 8~10회씩 중복 인덱싱되어 있다. 대화 로그 수집 파이프라인에서 중복 제거(deduplication) 로직이 누락된 상태다.

---

## 반복 등장 패턴

- **동일 세션의 다중 중복 인덱싱**: 3개의 고유 파일이 총 30개 항목으로 기록됨 → 로그 수집기의 중복 처리 버그가 지속적으로 미해결 상태
- **브라우저 익스텐션 미연결 상황 반복**: 세션 내용 수동 확인이 필요한 상황이 반복되고 있으며, 익스텐션 안정성이 워크플로우 병목 지점으로 작용
- **모델 설계 반영 여부 검증 루틴**: 새 작업 시작 전마다 "이전 세션 설계 반영 확인"이 선행 단계로 고정됨
- **API 인증 오류(401)**: sv_lakehouse 프로젝트에서 venv 활성화 시 인증 실패가 반복 발생

---

## 미해결 질문

- `session_01BmJwoQ8VR7J2YuHJipG9Qp`의 모델 설계가 실제 코드에 반영되었는지 최종 확인 여부가 불명확
- Multi-Agent Ecosystem 플랜에서 `nova_he*` 외 나머지 도메인 프로젝트 목록과 역할 분담이 문서화되지 않음
- sv_lakehouse의 API 401 오류 근본 원인(키 만료 vs 세션 토큰 문제 vs 환경변수 누락) 미확정
- 브라우저 익스텐션 미연결 시 세션 컨텍스트를 자동으로 복원하는 대체 메커니즘 부재
- 로그 중복 인덱싱 버그의 트리거 조건 미파악

---

## 관련 카테고리

- **PKB (Personal Knowledge Base)**: Multi-Agent 설계 플랜의 직접적 기
